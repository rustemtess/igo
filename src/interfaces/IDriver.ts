export interface IDriverData {
    driver_id: number,
    driver_name: string,
    driver_lastname: string,
    driver_phone_number: number,
    driver_kaspi_number: number,
    driver_car_model: string,
    driver_car_number: string
    driver_confirm: boolean,
    driver_registered: string
}